@page "/storeitem/{productId}"
@using MedbaseLibrary.Store

@code {
    [Parameter]
    public string? productId { get; set; }

    private Product product = new();
    private bool loading = true;
}
